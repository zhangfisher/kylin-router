---
phase: 02-hooks
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: [src/types.ts, src/features/hooks.ts, src/router.ts]
autonomous: true
requirements: [GUARD-02, GUARD-06, ERROR-04]
user_setup: []
must_haves:
  truths:
    - "renderEach 钩子在组件加载后、渲染前执行"
    - "renderEach 钩子可以预加载数据并通过 route.data 传递给组件"
    - "数据预加载失败时继续渲染组件，由组件处理错误状态"
    - "renderEach 钩子支持同步和异步数据加载"
    - "预加载的数据通过 route.data 对象传递给组件"
  artifacts:
    - path: "src/types.ts"
      provides: "数据预加载相关的类型定义"
      contains: "RouteData, renderEach"
    - path: "src/features/hooks.ts"
      provides: "renderEach 钩子的执行逻辑"
      exports: ["executeRenderEach"]
    - path: "src/router.ts"
      provides: "集成 renderEach 的导航流程"
      exports: ["onRouteUpdate"]
  key_links:
    - from: "src/router.ts"
      to: "src/types.ts"
      via: "route.data 数据传递"
      pattern: "route\.data.*=.*renderEach"
    - from: "src/features/hooks.ts"
      to: "src/router.ts"
      via: "executeRenderEach 方法调用"
      pattern: "executeRenderEach.*current\.route"
---

# Plan 02-03: 实现数据预加载机制

## 目标

实现 `renderEach` 钩子的数据预加载机制，在组件加载后、渲染前执行数据预取，支持通过 `route.data` 传递数据给组件。

目的：提供组件渲染前的数据预取能力，优化加载性能，改善用户体验。

输出：完整的 renderEach 钩子执行、数据传递和错误处理机制。

## 执行上下文

@E:/Work/Code/kylin-router/.claude/get-shit-done/workflows/execute-plan.md
@E:/Work/Code/kylin-router/.claude/get-shit-done/templates/summary.md

## 上下文

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hooks/02-CONTEXT.md

@.planning/phases/02-hooks/02-01-PLAN.md - 全局钩子系统
@.planning/phases/02-hooks/02-02-PLAN.md - 路由级守卫系统

### 参考实现

@src/features/hooks.ts - 前面计划实现的钩子系统
@src/router.ts - 集成了全局和路由级守卫的导航流程
@src/types.ts - RouteItem 类型定义

## 任务

<task type="auto">
  <name>Task 1: 定义 renderEach 钩子和数据类型</name>
  <files>src/types.ts</files>
  <read_first>
    - src/types.ts - 读取现有的 RouteItem 接口
    - .planning/phases/02-hooks/02-CONTEXT.md - 确认 D-18 到 D-20 决策
  </read_first>
  <action>
    在 src/types.ts 中定义 renderEach 钩子和数据传递相关的类型：

    1. 定义 RouteData 类型（用于传递预加载数据）：
       ```typescript
       export type RouteData = Record<string, any>;

      export interface EnhancedRouteItem extends RouteItem {
        /**
         * 预加载的数据，由 renderEach 钩子填充
         * 组件可以通过 route.data 访问这些数据
         */
        data?: RouteData;
      }
      ```

    2. 定义 renderEach 钩子函数类型：
       ```typescript
      export type RenderEachHook = (
        to: RouteItem,
        from: RouteItem,
        next: (data?: RouteData) => void,
        router: any
      ) => void | Promise<void> | RouteData | Promise<RouteData>;
      ```

    3. 在 KylinRouterOptions 中添加 renderEach 可选字段（如果尚未存在）：
       ```typescript
      export interface KylinRouterOptiopns {
        // ... 现有字段
        renderEach?: RenderEachHook | RenderEachHook[];
      }
      ```

    4. 更新 RouteItem 接口，添加 renderEach 可选字段：
       ```typescript
      export interface RouteItem {
        // ... 现有字段
        renderEach?: RenderEachHook | RenderEachHook[];
      }
      ```

    遵循 D-18 (组件加载后执行) 和 D-20 (通过 route.data 传递) 的决策
  </action>
  <verify>
    <automated>grep -n "export type RouteData" src/types.ts && grep -n "export type RenderEachHook" src/types.ts && grep -n "renderEach\?" src/types.ts</automated>
  </verify>
  <done>
    类型定义包含 RouteData 和 RenderEachHook，RouteItem 和 KylinRouterOptions 支持 renderEach 字段
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 renderEach 钩子执行逻辑</name>
  <files>src/features/hooks.ts</files>
  <read_first>
    - src/features/hooks.ts - 读取全局钩子的执行逻辑
    - src/types.ts - 确认 RenderEachHook 类型定义
  </read_first>
  <action>
    在 Hooks 类中实现 renderEach 钩子的执行方法（遵循 D-18 组件加载后执行）：

    1. 实现 executeRenderEach 方法：
       ```typescript
      protected async executeRenderEach(
        to: RouteItem,
        from: RouteItem,
        router: any
      ): Promise<RouteData | undefined> {
        // 收集全局 renderEach 钩子
        const globalHooks = this.hooks[HookType.RENDER_EACH] as RenderEachHook[];
        // 收集路由级 renderEach 钩子
        const routeHooks = to.renderEach
          ? Array.isArray(to.renderEach)
            ? to.renderEach
            : [to.renderEach]
          : [];

        const allHooks = [...globalHooks, ...routeHooks];
        if (allHooks.length === 0) return undefined;

        let combinedData: RouteData = {};

        // 串行执行所有 renderEach 钩子
        for (const hook of allHooks) {
          try {
            const result = await this.runRenderEachHook(hook, to, from, router);
            if (result) {
              combinedData = { ...combinedData, ...result };
            }
          } catch (error) {
            console.error('[Router] renderEach hook error:', error);
            // 不抛出错误，继续执行（D-19）
          }
        }

        return combinedData;
      }
      ```

    2. 实现 runRenderEachHook 方法（处理单个钩子）：
       ```typescript
      protected async runRenderEachHook(
        hook: RenderEachHook,
        to: RouteItem,
        from: RouteItem,
        router: any
      ): Promise<RouteData | undefined> {
        return new Promise((resolve) => {
          const timeout = setTimeout(() => {
            console.error('[Router] renderEach hook timeout after 30000ms');
            resolve(undefined); // 超时返回 undefined，不阻塞导航
          }, 30000);

          const next = (data?: RouteData) => {
            clearTimeout(timeout);
            resolve(data);
          };

          try {
            const result = hook(to, from, next, router);

            // 支持直接返回数据
            if (result instanceof Promise) {
              result.then(data => {
                clearTimeout(timeout);
                resolve(data);
              }).catch(error => {
                clearTimeout(timeout);
                console.error('[Router] renderEach hook execution error:', error);
                resolve(undefined);
              });
            } else if (result !== undefined) {
              clearTimeout(timeout);
              resolve(result);
            }
            // 如果调用 next()，由 next 回调处理
          } catch (error) {
            clearTimeout(timeout);
            console.error('[Router] renderEach hook sync error:', error);
            resolve(undefined);
          }
        });
      }
      ```

    遵循 D-19 (失败时继续渲染) 和 D-20 (通过 route.data 传递) 的决策
  </action>
  <verify>
    <automated>grep -n "protected async executeRenderEach" src/features/hooks.ts && grep -n "protected async runRenderEachHook" src/features/hooks.ts</automated>
  </verify>
  <done>
    Hooks 类包含 renderEach 钩子执行逻辑，支持数据合并和错误处理
  </done>
</task>

<task type="auto">
  <name>Task 3: 集成 renderEach 到导航流程</name>
  <files>src/router.ts</files>
  <read_first>
    - src/router.ts - 读取当前 onRouteUpdate 方法的实现
    - src/features/hooks.ts - 确认 executeRenderEach 方法的签名
  </read_first>
  <action>
    在 KylinRouter 的导航流程中集成 renderEach 执行：

    1. 在路由匹配、守卫执行后，组件渲染前执行 renderEach：
       ```typescript
      onRouteUpdate(location: Update) {
        this.isNavigating = true;
        const pathname = location.location.pathname;
        const search = location.location.search;

        // ... 全局 beforeEach 执行 ...

        // ... 路由匹配 ...

         // ... 路由级 beforeEnter 执行 ...

        // 执行 renderEach 钩子（数据预加载）
        const renderData = await this.executeRenderEach(
          this.current.route,
          { name: '', path: '', params: {}, query: {} }, // from
          this
        );

        // 将预加载的数据存储到 route.data
        if (renderData) {
          (this.current.route as any).data = renderData;
        }

        // ... 继续组件渲染 ...
      }
      ```

    2. 确保 route.data 在渲染组件时可用：
       - 数据将存储在 current.route.data 中
       - 组件可以通过 this.route.data 访问预加载的数据

    3. 处理 renderEach 执行失败的情况（遵循 D-19）：
       - 失败时不阻塞导航
       - 记录错误到控制台
       - 组件负责处理缺失数据的场景

    遵循 D-18 (组件加载后执行) 和 D-19 (失败时继续渲染) 的决策
  </action>
  <verify>
    <automated>grep -n "executeRenderEach" src/router.ts && grep -n "route\.data" src/router.ts</automated>
  </verify>
  <done>
    onRouteUpdate 方法在组件渲染前执行 renderEach 钩子，数据存储在 route.data 中
  </done>
</task>

<task type="auto">
  <name>Task 4: 实现钩子错误边界和恢复机制</name>
  <files>src/features/hooks.ts, src/router.ts</files>
  <read_first>
    - src/features/hooks.ts - 读取 renderEach 的错误处理
    - .planning/phases/02-hooks/02-CONTEXT.md - 确认 D-29 和 D-30 决策
  </read_first>
  <action>
    实现 renderEach 钩子的错误边界和恢复机制：

    1. 在 executeRenderEach 中添加错误统计和恢复：
       ```typescript
      protected async executeRenderEach(
        to: RouteItem,
        from: RouteItem,
        router: any
      ): Promise<RouteData | undefined> {
        const allHooks = [...globalHooks, ...routeHooks];
        if (allHooks.length === 0) return undefined;

        let combinedData: RouteData = {};
        let errorCount = 0;
        const errors: Array<{ hook: RenderEachHook; error: Error }> = [];

        for (const hook of allHooks) {
          try {
            const result = await this.runRenderEachHook(hook, to, from, router);
            if (result) {
              combinedData = { ...combinedData, ...result };
            }
          } catch (error) {
            errorCount++;
            errors.push({ hook, error: error as Error });
            console.error('[Router] renderEach hook failed:', {
              route: to.name,
              error
            });
            // 继续执行下一个钩子（D-19）
          }
        }

        // 记录错误统计
        if (errorCount > 0) {
          console.warn(`[Router] ${errorCount} renderEach hooks failed, continuing navigation`);
        }

        return combinedData;
      }
      ```

    2. 添加错误恢复机制（可选的重试逻辑）：
       ```typescript
      protected async runRenderEachHookWithRetry(
        hook: RenderEachHook,
        to: RouteItem,
        from: RouteItem,
        router: any,
        maxRetries: number = 1
      ): Promise<RouteData | undefined> {
        for (let attempt = 0; attempt <= maxRetries; attempt++) {
          try {
            return await this.runRenderEachHook(hook, to, from, router);
          } catch (error) {
            if (attempt === maxRetries) {
              throw error;
            }
            console.warn(`[Router] renderEach hook retry ${attempt + 1}/${maxRetries}`);
            await new Promise(resolve => setTimeout(resolve, 1000)); // 延迟重试
          }
        }
        return undefined;
      }
      ```

    3. 确保错误不影响组件渲染：
       - renderEach 失败后仍然触发组件渲染
       - 组件通过检查 route.data 是否存在来处理错误情况

    遵循 D-29 (混合错误处理) 和 D-30 (自动错误日志) 的决策
  </action>
  <verify>
    <automated>grep -n "errorCount\|errors\.push" src/features/hooks.ts && grep -n "runRenderEachHookWithRetry" src/features/hooks.ts</automated>
  </verify>
  <done>
    renderEach 钩子有完整的错误边界、恢复机制和日志记录
  </done>
</task>

<task type="auto">
  <name>Task 5: 实现钩子与路由状态的集成</name>
  <files>src/router.ts</files>
  <read_first>
    - src/router.ts - 读取 current 对象的结构
    - src/types.ts - 确认需要扩展的类型
  </read_first>
  <action>
    实现钩子与路由状态的集成，确保数据正确传递：

    1. 扩展 current 对象以包含预加载数据：
       ```typescript
      export interface RouteState {
        route: RouteItem & { data?: RouteData };
        params: Record<string, string>;
        query: Record<string, string>;
        matched?: MatchedRoute[];
      }

      export class KylinRouter extends Mixin(...) {
        public current: RouteState = {
          route: {} as any,
          params: {},
          query: {}
        };
      }
      ```

    2. 在导航流程中正确传递数据：
       ```typescript
      onRouteUpdate(location: Update) {
        // ... renderEach 执行 ...

        // 更新 current 对象
        this.current = {
          route: {
            ...matchedRoute.route,
            data: renderData // 预加载的数据
          },
          params: matchedRoute.params,
          query: this.parseQuery(search),
          matched: matchedRoutes
        };

        // 触发 route-change 事件（包含 data）
        this.host.dispatchEvent(new CustomEvent("route-change", {
          detail: {
            route: this.current.route,
            params: this.current.params,
            query: this.current.query,
            location: location
          },
          bubbles: true
        }));
      }
      ```

    3. 确保组件可以访问预加载的数据：
       - 组件通过 router.current.route.data 访问数据
       - 或通过路由事件 detail.route.data 访问

    遵循 D-26 (通过 router 参数访问状态) 和 D-27 (通过 next 传递状态) 的决策
  </action>
  <verify>
    <automated>grep -n "current: RouteState" src/router.ts && grep -n "data: renderData" src/router.ts</automated>
  </verify>
  <done>
    current 对象包含预加载数据，组件可以通过 router.current.route.data 访问
  </done>
</task>

## 威胁模型

### 信任边界

| Boundary | Description |
|----------|-------------|
| renderEach 钩子 → 路由数据 | 钩子可能返回恶意数据或超大对象 |
| route.data → 组件 | 组件可能依赖不存在的预加载数据 |

### STRIDE 威胁登记

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Spoofing | 预加载数据 | mitigate | 数据由钩子提供，应用层负责验证 |
| T-02-13 | Tampering | 数据合并 | mitigate | 使用浅合并避免原型污染 |
| T-02-14 | Denial of Service | 内存泄漏 | mitigate | 限制预加载数据大小，导航时清理 |
| T-02-15 | Information Disclosure | 敏感数据 | accept | 数据在应用内传递，信任边界明确 |

## 验证

### 整体阶段检查

- [ ] renderEach 钩子在正确的时机执行（组件加载后）
- [ ] 预加载数据通过 route.data 正确传递
- [ ] renderEach 失败时不阻塞组件渲染
- [ ] 错误处理和日志记录完整
- [ ] 组件可以访问预加载的数据
- [ ] 所有测试通过

## 成功标准

### 可测量的完成标准

1. **功能完整性**：
   - [ ] renderEach 钩子在组件渲染前执行
   - [ ] 预加载数据通过 route.data 传递给组件
   - [ ] 支持同步和异步数据加载
   - [ ] 多个 renderEach 钩子的数据正确合并

2. **错误处理**：
   - [ ] renderEach 失败时继续渲染组件
   - [ ] 错误自动记录到控制台
   - [ ] 超时机制防止挂起

3. **兼容性**：
   - [ ] 与全局钩子协同工作
   - [ ] 与路由级守卫协同工作
   - [ ] 所有现有测试通过

## 输出

完成后创建 `.planning/phases/02-hooks/02-03-SUMMARY.md`

---

**需求覆盖**：GUARD-02 (beforeResolve/data preloading), GUARD-06 (数据预加载机制), ERROR-04 (循环检测 - 复用)

**决策覆盖**：
- D-18: renderEach 在组件加载后执行 ✓
- D-19: 失败时继续渲染 ✓
- D-20: 通过 route.data 传递 ✓
- D-26: 通过 router 参数访问状态 ✓
- D-27: 通过 next 传递状态 ✓
- D-29: 混合错误处理模式 ✓
- D-30: 自动错误日志 ✓
