---
phase: 02-hooks
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified: [src/features/hooks.ts, src/router.ts, src/__tests__]
autonomous: false
requirements: [GUARD-01, GUARD-02, GUARD-03, CORE-05, CORE-06, UX-06, UX-07]
user_setup: []
must_haves:
  truths:
    - "所有三种钩子类型（beforeEach、renderEach、afterEach）完整集成到导航流程"
    - "路由级守卫（beforeEnter、afterLeave）与全局钩子协同工作"
    - "嵌套路由的守卫执行顺序正确，父路由守卫优先执行"
    - "数据预加载机制正常工作，预加载数据传递给组件"
    - "所有错误处理机制正常，包括超时、循环检测和错误边界"
    - "完整的测试覆盖所有钩子功能"
  artifacts:
    - path: "src/__tests__/router.hooks.test.ts"
      provides: "钩子系统的完整测试覆盖"
      min_lines: 300
    - path: "src/__tests__/router.guards.test.ts"
      provides: "路由级守卫的测试覆盖"
      min_lines: 200
    - path: "example/public/app/hooks-demo.html"
      provides: "钩子系统使用示例"
      min_lines: 100
  key_links:
    - from: "src/__tests__/router.hooks.test.ts"
      to: "src/features/hooks.ts"
      via: "测试所有钩子执行逻辑"
      pattern: "describe.*hooks.*test"
    - from: "example/public/app/hooks-demo.html"
      to: "src/router.ts"
      via: "演示钩子系统的实际使用"
      pattern: "router\.hooks\."
---

# Plan 02-04: 完善钩子系统集成、测试和文档

## 目标

完善钩子系统的集成，确保所有钩子类型正确协同工作，编写完整的测试覆盖，并提供使用示例文档。

目的：验证钩子系统的功能完整性、稳定性和可用性，为开发者提供清晰的使用指南。

输出：完整的测试套件、使用示例和集成验证。

## 执行上下文

@E:/Work/Code/kylin-router/.claude/get-shit-done/workflows/execute-plan.md
@E:/Work/Code/kylin-router/.claude/get-shit-done/templates/summary.md

## 上下文

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hooks/02-CONTEXT.md

@.planning/phases/02-hooks/02-01-PLAN.md - 全局钩子系统
@.planning/phases/02-hooks/02-02-PLAN.md - 路由级守卫系统
@.planning/phases/02-hooks/02-03-PLAN.md - 数据预加载机制

### 参考实现

@src/features/hooks.ts - 完整的钩子系统实现
@src/router.ts - 集成了所有钩子的导航流程
@src/__tests__/router.test.ts - 现有测试参考

## 任务

<task type="auto">
  <name>Task 1: 编写全局钩子系统的测试</name>
  <files>src/__tests__/router.hooks.test.ts</files>
  <read_first>
    - src/__tests__/router.test.ts - 参考现有测试模式
    - src/features/hooks.ts - 确认需要测试的方法
    - src/types.ts - 确认钩子类型定义
  </read_first>
  <action>
    创建 `src/__tests__/router.hooks.test.ts`，编写全局钩子的完整测试：

    1. 测试 beforeEach 钩子：
       ```typescript
       describe('beforeEach hooks', () => {
         it('should execute beforeEach hooks in registration order', async () => {
           const order: number[] = [];
           router.hooks.beforeEach.push(() => { order.push(1); });
           router.hooks.beforeEach.push(() => { order.push(2); });
           // 执行导航并验证顺序
           expect(order).toEqual([1, 2]);
         });

         it('should cancel navigation when next(false) is called', async () => {
           router.hooks.beforeEach.push((to, from, next) => {
             next(false);
           });
           await router.push('/protected');
           expect(router.current.route.path).not.toBe('/protected');
         });

         it('should redirect when next("/path") is called', async () => {
           router.hooks.beforeEach.push((to, from, next) => {
             if (to.path === '/redirect') {
               next('/target');
             } else {
               next();
             }
           });
           await router.push('/redirect');
           expect(router.current.route.path).toBe('/target');
         });

         it('should support async beforeEach hooks', async () => {
           let executed = false;
           router.hooks.beforeEach.push(async (to, from, next) => {
             await new Promise(resolve => setTimeout(resolve, 100));
             executed = true;
             next();
           });
           await router.push('/async');
           expect(executed).toBe(true);
         });

         it('should timeout after 30 seconds', async () => {
           router.hooks.beforeEach.push(() => {
             // Never call next()
           });
           // 测试超时逻辑
         }, 35000);
       });
       ```

    2. 测试 renderEach 钩子：
       ```typescript
       describe('renderEach hooks', () => {
         it('should execute renderEach hooks and pass data to component', async () => {
           router.hooks.renderEach.push((to, from, next) => {
             next({ userId: '123', userName: 'Test' });
           });
           await router.push('/user');
           expect(router.current.route.data).toEqual({
             userId: '123',
             userName: 'Test'
           });
         });

         it('should merge data from multiple renderEach hooks', async () => {
           router.hooks.renderEach.push(() => ({ key1: 'value1' }));
           router.hooks.renderEach.push(() => ({ key2: 'value2' }));
           await router.push('/merge');
           expect(router.current.route.data).toEqual({
             key1: 'value1',
             key2: 'value2'
           });
         });
       });
       ```

    3. 测试 afterEach 钩子：
       ```typescript
       describe('afterEach hooks', () => {
         it('should execute afterEach hooks after successful navigation', async () => {
           let executed = false;
           router.hooks.afterEach.push(() => {
             executed = true;
           });
           await router.push('/target');
           expect(executed).toBe(true);
         });

         it('should not execute afterEach when navigation is canceled', async () => {
           let executed = false;
           router.hooks.beforeEach.push((to, from, next) => next(false));
           router.hooks.afterEach.push(() => { executed = true; });
           await router.push('/protected');
           expect(executed).toBe(false);
         });
       });
       ```

    4. 测试重定向循环检测：
       ```typescript
       describe('redirect cycle detection', () => {
         it('should detect infinite redirect loops', async () => {
           let count = 0;
           router.hooks.beforeEach.push((to, from, next) => {
             if (count < 15) {
               count++;
               next('/loop');
             } else {
               next();
             }
           });
           await router.push('/start');
           // 验证超过10次后停止
           expect(count).toBeLessThanOrEqual(10);
         });
       });
       ```

    遵循现有的测试模式和命名约定
  </action>
  <verify>
    <automated>bun test src/__tests__/router.hooks.test.ts</automated>
  </verify>
  <done>
    全局钩子测试覆盖所有功能和边界情况
  </done>
</task>

<task type="auto">
  <name>Task 2: 编写路由级守卫的测试</name>
  <files>src/__tests__/router.guards.test.ts</files>
  <read_first>
    - src/__tests__/router.hooks.test.ts - 参考全局钩子测试模式
    - src/types.ts - 确认 beforeEnter 和 afterLeave 类型
  </read_first>
  <action>
    创建 `src/__tests__/router.guards.test.ts`，编写路由级守卫的完整测试：

    1. 测试 beforeEnter 守卫：
       ```typescript
       describe('beforeEnter guards', () => {
         it('should execute beforeEnter guard before entering route', async () => {
           let guardExecuted = false;
           const routes = [
             {
               name: 'protected',
               path: '/protected',
               beforeEnter: () => {
                 guardExecuted = true;
                 return true;
               }
             }
           ];
           const router = new KylinRouter(host, { routes });
           await router.push('/protected');
           expect(guardExecuted).toBe(true);
         });

         it('should cancel navigation when beforeEnter returns false', async () => {
           const routes = [
             {
               name: 'protected',
               path: '/protected',
               beforeEnter: () => false
             }
           ];
           await router.push('/protected');
           expect(router.current.route.path).not.toBe('/protected');
         });

         it('should redirect when beforeEnter returns path string', async () => {
           const routes = [
             {
               name: 'redirect',
               path: '/redirect',
               beforeEnter: () => '/target'
             },
             {
               name: 'target',
               path: '/target'
             }
           ];
           await router.push('/redirect');
           expect(router.current.route.path).toBe('/target');
         });
       });
       ```

    2. 测试嵌套路由的守卫执行顺序：
       ```typescript
       describe('nested route guards', () => {
         it('should execute parent guards before child guards', async () => {
           const order: string[] = [];
           const routes = [
             {
               name: 'parent',
               path: '/parent',
               beforeEnter: () => {
                 order.push('parent');
                 return true;
               },
               children: [
                 {
                   name: 'child',
                   path: 'child',
                   beforeEnter: () => {
                     order.push('child');
                     return true;
                   }
                 }
               ]
             }
           ];
           await router.push('/parent/child');
           expect(order).toEqual(['parent', 'child']);
         });

         it('should fallback to parent route when child guard fails', async () => {
           const routes = [
             {
               name: 'parent',
               path: '/parent',
               children: [
                 {
                   name: 'child',
                   path: 'child',
                   beforeEnter: () => false
                 }
               ]
             }
           ];
           await router.push('/parent/child');
           expect(router.current.route.path).toBe('/parent');
         });
       });
       ```

    3. 测试 afterLeave 守卫：
       ```typescript
       describe('afterLeave guards', () => {
         it('should execute afterLeave guard after leaving route', async () => {
           let guardExecuted = false;
           const routes = [
             {
               name: 'source',
               path: '/source',
               afterLeave: () => {
                 guardExecuted = true;
               }
             },
             {
               name: 'target',
               path: '/target'
             }
           ];
           await router.push('/source');
           await router.push('/target');
           expect(guardExecuted).toBe(true);
         });
       });
       ```

    遵循现有的测试模式和命名约定
  </action>
  <verify>
    <automated>bun test src/__tests__/router.guards.test.ts</automated>
  </verify>
  <done>
    路由级守卫测试覆盖所有功能和嵌套场景
  </done>
</task>

<task type="auto">
  <name>Task 3: 验证钩子系统的完整集成</name>
  <files>src/__tests__/router.integration.test.ts</files>
  <read_first>
    - src/router.ts - 读取完整的导航流程
    - src/features/hooks.ts - 确认所有钩子执行点
  </read_first>
  <action>
    创建 `src/__tests__/router.integration.test.ts`，编写完整的集成测试：

    1. 测试完整的导航流程：
       ```typescript
       describe('complete navigation flow with hooks', () => {
         it('should execute all hooks in correct order', async () => {
           const order: string[] = [];
           const routes = [
             {
               name: 'target',
               path: '/target',
               beforeEnter: () => { order.push('beforeEnter'); return true; }
             }
           ];

           router.hooks.beforeEach.push(() => { order.push('beforeEach'); });
           router.hooks.renderEach.push(() => { order.push('renderEach'); });
           router.hooks.afterEach.push(() => { order.push('afterEach'); });

           await router.push('/target');

           expect(order).toEqual([
             'beforeEach',
             'beforeEnter',
             'renderEach',
             'afterEach'
           ]);
         });
       });
       ```

    2. 测试钩子与路由状态的交互：
       ```typescript
       it('should pass correct to/from parameters to hooks', async () => {
         const routes = [
           { name: 'a', path: '/a' },
           { name: 'b', path: '/b' }
         ];

         let capturedTo: any, capturedFrom: any;
         router.hooks.beforeEach.push((to, from, next) => {
           capturedTo = to;
           capturedFrom = from;
           next();
         });

         await router.push('/a');
         await router.push('/b');

         expect(capturedTo.name).toBe('b');
         expect(capturedFrom.name).toBe('a');
       });
       ```

    3. 测试错误处理和恢复：
       ```typescript
       it('should handle hook errors gracefully', async () => {
         router.hooks.beforeEach.push(() => {
           throw new Error('Hook error');
         });
         router.hooks.beforeEach.push((to, from, next) => {
           next(); // 第二个钩子应该继续执行
         });

         // 不应该抛出错误，导航应该继续
         await expect(router.push('/target')).resolves.not.toThrow();
       });
       ```

    遵循现有的测试模式和命名约定
  </action>
  <verify>
    <automated>bun test src/__tests__/router.integration.test.ts</automated>
  </verify>
  <done>
    集成测试验证所有钩子协同工作的正确性
  </done>
</task>

<task type="auto">
  <name>Task 4: 创建钩子系统使用示例</name>
  <files>example/public/app/hooks-demo.html</files>
  <read_first>
    - example/public/app/index.html - 参考现有示例结构
    - src/router.ts - 确认钩子 API
  </read_first>
  <action>
    创建 `example/public/app/hooks-demo.html`，提供钩子系统的使用示例：

    1. 展示全局钩子的使用：
       ```html
       <!DOCTYPE html>
       <html>
       <head>
         <title>Router Hooks Demo</title>
         <script type="module" src="../../src/index.ts"></script>
       </head>
       <body>
         <div id="app">
           <nav>
             <kylin-link href="/">Home</kylin-link>
             <kylin-link href="/profile">Profile</kylin-link>
             <kylin-link href="/admin">Admin</kylin-link>
           </nav>
           <kylin-outlet></kylin-outlet>
         </div>

         <script type="module">
           import { KylinRouter } from '../../src/router.js';

           const router = new KylinRouter(document.getElementById('app'), {
             routes: [
               {
                 name: 'home',
                 path: '/',
                 component: '<h1>Home</h1>'
               },
               {
                 name: 'profile',
                 path: '/profile',
                 beforeEnter: (to, from) => {
                   // 路由级守卫：检查认证
                   if (!isAuthenticated()) {
                     return '/login';
                   }
                   return true;
                 },
                 renderEach: (to, from, next) => {
                   // 数据预加载
                   next({ userProfile: loadUserProfile() });
                 },
                 component: '<h1>Profile</h1><p>User: {{userProfile.name}}</p>'
               },
               {
                 name: 'admin',
                 path: '/admin',
                 beforeEnter: (to, from) => {
                   // 权限检查
                   if (!hasAdminRole()) {
                     return '/unauthorized';
                   }
                   return true;
                 },
                 component: '<h1>Admin Panel</h1>'
               }
             ]
           });

           // 全局 beforeEach：日志记录
           router.hooks.beforeEach.push((to, from, next) => {
             console.log(`Navigating from ${from.path} to ${to.path}`);
             next();
           });

           // 全局 beforeEach：认证检查
           router.hooks.beforeEach.push((to, from, next) => {
             if (to.meta?.requiresAuth && !isAuthenticated()) {
               next('/login');
             } else {
               next();
             }
           });

           // 全局 renderEach：页面标题设置
           router.hooks.renderEach.push((to, from, next) => {
             document.title = to.meta?.title || 'My App';
             next();
           });

           // 全局 afterEach：滚动到顶部
           router.hooks.afterEach.push((to, from) => {
             window.scrollTo(0, 0);
           });

           // 全局 afterEach：分析追踪
           router.hooks.afterEach.push((to, from) => {
             trackPageView(to.path);
           });

           // 辅助函数
           function isAuthenticated() {
             return localStorage.getItem('token') !== null;
           }

           function hasAdminRole() {
             return localStorage.getItem('role') === 'admin';
           }

           function loadUserProfile() {
             return { name: 'John Doe', email: 'john@example.com' };
           }

           function trackPageView(path) {
             console.log('Page view:', path);
           }
         </script>
       </body>
       </html>
       ```

    2. 展示异步钩子的使用：
       ```javascript
       // 异步权限验证
       router.hooks.beforeEach.push(async (to, from, next) => {
             if (to.meta?.requiresAuth) {
               const isValid = await validateToken();
               if (!isValid) {
                 next('/login');
                 return;
               }
             }
             next();
           });
       ```

    3. 展示数据预加载的使用：
       ```javascript
       // 数据预加载
           next({ users: await fetchUsers() });
         });
       ```

    提供清晰、实用的示例代码，涵盖常见使用场景
  </action>
  <verify>
    <automated>grep -n "router.hooks" example/public/app/hooks-demo.html && grep -n "beforeEnter\|renderEach" example/public/app/hooks-demo.html</automated>
  </verify>
  <done>
    示例文件包含钩子系统的常见使用场景
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: 验证钩子系统的完整性和用户体验</name>
  <what-built>
    - 全局钩子系统（beforeEach、renderEach、afterEach）
    - 路由级守卫系统（beforeEnter、afterLeave）
    - 数据预加载机制（renderEach + route.data）
    - 完整的错误处理和超时机制
    - 重定向循环检测
    - 完整的测试覆盖
    - 使用示例和文档
  </what-built>
  <how-to-verify>
    1. 打开浏览器访问 `example/public/app/hooks-demo.html`
    2. 测试以下场景：
       - 点击导航链接，观察全局钩子执行（控制台日志）
       - 访问受保护的路由（/profile），验证 beforeEnter 守卫
       - 访问管理员路由（/admin），验证权限检查
       - 检查页面标题是否正确更新（renderEach 钩子）
       - 验证导航后页面滚动到顶部（afterEach 钩子）
       - 尝试触发重定向循环，验证错误处理
    3. 运行所有测试：
       ```bash
       bun test src/__tests__/router.hooks.test.ts
       bun test src/__tests__/router.guards.test.ts
       bun test src/__tests__/router.integration.test.ts
       ```
    4. 验证所有测试通过
    5. 检查控制台日志，确认钩子执行顺序正确
  </how-to-verify>
  <resume-signal>输入 "approved" 表示验证通过，或描述发现的问题</resume-signal>
</task>

## 威胁模型

### 信任边界

| Boundary | Description |
|----------|-------------|
| 示例代码 → 用户应用 | 示例代码可能包含安全漏洞或错误模式 |
| 测试代码 → 生产代码 | 测试可能暴露内部实现细节 |

### STRIDE 威胁登记

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Spoofing | 示例代码 | accept | 示例代码仅供参考，生产环境需自行验证 |
| T-02-17 | Tampering | 测试依赖 | mitigate | 测试独立运行，不影响生产代码 |
| T-02-18 | Information Disclosure | 日志输出 | mitigate | 示例中的日志不包含敏感信息 |

## 验证

### 整体阶段检查

- [ ] 所有三种钩子类型完整集成
- [ ] 路由级守卫与全局钩子协同工作
- [ ] 嵌套路由的守卫执行顺序正确
- [ ] 数据预加载机制正常工作
- [ ] 错误处理和恢复机制完整
- [ ] 测试覆盖所有功能和边界情况
- [ ] 使用示例清晰且实用
- [ ] 所有测试通过

## 成功标准

### 可测量的完成标准

1. **功能完整性**：
   - [ ] beforeEach、renderEach、afterEach 钩子全部实现
   - [ ] beforeEnter、afterLeave 守卫全部实现
   - [ ] 嵌套路由守卫执行顺序正确
   - [ ] 数据预加载和传递机制正常

2. **测试覆盖**：
   - [ ] 全局钩子测试覆盖率 > 90%
   - [ ] 路由级守卫测试覆盖率 > 90%
   - [ ] 集成测试覆盖所有导航场景
   - [ ] 所有测试通过

3. **文档和示例**：
   - [ ] 使用示例包含所有钩子类型
   - [ ] 示例代码清晰易懂
   - [ ] 涵盖常见使用场景

4. **兼容性**：
   - [ ] 与现有路由功能完全兼容
   - [ ] 所有现有测试仍然通过
   - [ ] 性能影响最小

## 输出

完成后创建 `.planning/phases/02-hooks/02-04-SUMMARY.md`

---

**需求覆盖**：GUARD-01 (beforeEach), GUARD-02 (renderEach), GUARD-03 (afterEach), CORE-05 (嵌套路由), CORE-06 (outlet - Phase 3), UX-06 (命名 outlet - Phase 3), UX-07 (滚动行为 - 通过 afterEach)

**决策覆盖**：
- D-01 到 D-32: 所有钩子相关决策完整实现 ✓
- D-28: 文档和示例 ✓
- D-29: 混合错误处理 ✓
- D-30: 自动错误日志 ✓
