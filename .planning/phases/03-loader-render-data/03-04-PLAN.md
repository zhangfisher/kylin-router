---
phase: 03-loader-render-data
plan: 04
type: execute
wave: 4
depends_on: [03-02, 03-03]
files_modified: [src/router.ts, src/types.ts, src/components/outlet/index.ts, example/public/app/modal-demo.html]
autonomous: false
requirements: [MODAL-01, MODAL-02, MODAL-03]
user_setup: []
must_haves:
  truths:
    - 路由器支持模态路由渲染，在专用容器中打开模态框
    - 模态路由支持背景遮罩和关闭交互
    - 模态路由可以与普通路由共存
    - 支持多层模态栈，新模态覆盖旧模态
    - 用户可以通过 router.back() 或 router.closeModal() 关闭模态
  artifacts:
    - path: src/router.ts
      provides: KylinRouter 类，集成模态路由管理
      min_lines: 300（修改）
      exports: [openModal, closeModal, closeTopModal]
    - path: src/types.ts
      provides: 模态路由相关的类型定义
      contains: ModalConfig, ModalStack, ModalOptions
    - path: src/components/outlet/index.ts
      provides: KylinOutletElement 组件，支持模态路由
      min_lines: 100（修改）
    - path: example/public/app/modal-demo.html
      provides: 模态路由使用示例
      min_lines: 200
  key_links:
    - from: src/router.ts
      to: .kylin-modals container
      via: 创建和管理模态容器
      pattern: createElement\('div'\).*kylin-modals
    - from: src/router.ts
      to: src/components/outlet/index.ts
      via: 模态路由渲染到专用 outlet
      pattern: renderToOutlet.*modal
    - from: src/components/outlet/index.ts
      to: src/router.ts
      via: 监听模态路由变化
      pattern: addEventListener\('modal-change'.*
---

<objective>
实现模态路由系统，支持模态框管理、多层模态栈、背景遮罩和关闭交互

Purpose: 提供完整的模态路由功能，支持与普通路由共存的模态框管理
Output: 模态路由管理系统，包括模态容器、模态栈、背景遮罩和关闭逻辑
</objective>

<execution_context>
@C:/Work/Code/kylin-router/.claude/get-shit-done/workflows/execute-plan.md
@C:/Work/Code/kylin-router/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-loader-render-data/03-CONTEXT.md
@.planning/phases/03-loader-render-data/03-02-PLAN.md
@.planning/phases/03-loader-render-data/03-03-PLAN.md
@.planning/phases/02-hooks/02-04-SUMMARY.md
@.planning/ROADMAP.md

## 关键决策回顾

从 03-CONTEXT.md 中锁定的决策：
- **D-16**: 使用路由配置中的 modal 字段声明模态路由
- **D-17**: 模态路由渲染到 host 元素下的 <div class="kylin-modals"> 容器
- **D-18**: 模态路由的背景遮罩通过 backdrop 配置字段控制
- **D-19**: 支持多层模态栈，新模态覆盖旧模态
- **D-20**: 用户可以通过 router.back() 或 router.closeModal() 关闭模态

## Plan 03-02 和 03-03 集成点

- Render 类已实现组件渲染（支持替换和追加模式）
- DataLoader 类已实现错误处理和重试机制
- Outlet 组件支持路由变化监听和自动渲染

## Phase 1 导航系统集成

从 01-01-SUMMARY.md：
- 路由匹配算法支持静态、动态和嵌套路由
- 导航方法：push、replace、back、forward

## 现有代码结构

- `src/router.ts`: KylinRouter 主类，有 History API 和导航方法
- `src/components/outlet/index.ts`: KylinOutletElement 组件
- `src/features/render.ts`: Render 类，支持多种渲染模式
- `src/features/context.ts`: Context 传播系统
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义模态路由相关的类型系统</name>
  <files>src/types.ts</files>
  <action>
在 src/types.ts 中添加模态路由相关的类型定义：

1. **ModalConfig 接口** - 模态路由配置
   ```typescript
   interface ModalConfig {
       modal: boolean;           // 是否为模态路由
       backdrop?: boolean;       // 是否显示背景遮罩（默认 true）
       closeOnBackdropClick?: boolean;  // 点击遮罩是否关闭（默认 true）
       closeOnEsc?: boolean;     // 按 ESC 键是否关闭（默认 true）
       stackable?: boolean;      // 是否支持多层模态（默认 true）
   }
   ```

2. **ModalStack 接口** - 模态栈项
   ```typescript
   interface ModalStackItem {
       route: RouteItem;         // 模态路由对象
       element: HTMLElement;     // 模态元素
       backdrop?: HTMLElement;   // 背景遮罩元素
       timestamp: number;        // 创建时间戳
   }
   ```

3. **ModalOptions 接口** - 打开模态的选项
   ```typescript
   interface ModalOptions {
       route?: string | RouteItem;  // 路由路径或配置
       params?: Record<string, string>;  // 路由参数
       query?: Record<string, string>;  // 查询参数
       backdrop?: boolean;         // 是否显示背景遮罩
   }
   ```

4. **ModalState 类型** - 模态状态
   ```typescript
   type ModalState = {
       stack: ModalStackItem[];   // 模态栈
       current: ModalStackItem | null;  // 当前模态
   };
   ```

5. 在 RouteItem 接口中扩展字段
   ```typescript
   modal?: boolean | ModalConfig;  // 模态路由配置
   ```

6. 在 KylinRouterOptions 接口中扩展字段
   ```typescript
   modalContainer?: string;  // 模态容器选择器（默认 '.kylin-modals'）
   ```

提供完整的 JSDoc 文档和类型注解。
  </action>
  <verify>grep -n "ModalConfig\|ModalStack\|ModalOptions\|ModalState" src/types.ts</verify>
  <done>模态路由类型定义完整，包含所有必需字段和文档</done>
</task>

<task type="auto">
  <name>Task 2: 实现 KylinRouter 的模态容器管理</name>
  <files>src/router.ts</files>
  <action>
在 KylinRouter 类中添加模态容器管理功能：

**新增属性：**
```typescript
private modalContainer: HTMLElement | null = null;
private modalState: ModalState = {
    stack: [],
    current: null
};
private maxModals: number = 10;  // 最大模态层数，防止无限堆叠
```

**实现方法：**

1. **创建模态容器（D-17）**
   ```typescript
   private createModalContainer(): HTMLElement {
       let container = this.host.querySelector('.kylin-modals') as HTMLElement;
       if (!container) {
           container = document.createElement('div');
           container.className = 'kylin-modals';
           this.host.appendChild(container);
       }
       this.modalContainer = container;
       return container;
   }
   ```

2. **初始化模态容器**
   ```typescript
   private initModals() {
       this.createModalContainer();
       this.setupModalEventListeners();
   }
   ```

3. **设置模态事件监听**
   ```typescript
   private setupModalEventListeners() {
       // ESC 键关闭模态
       document.addEventListener('keydown', (e) => {
           if (e.key === 'Escape' && this.modalState.current) {
               const config = this.getModalConfig(this.modalState.current.route);
               if (config?.closeOnEsc !== false) {
                   this.closeTopModal();
               }
           }
       });
   }
   ```

4. **获取模态配置**
   ```typescript
   private getModalConfig(route: RouteItem): ModalConfig | null {
       if (typeof route.modal === 'boolean') {
           return route.modal ? { modal: true } : null;
       }
       return route.modal || null;
   }
   ```

5. **添加默认样式**
   ```typescript
   private injectModalStyles() {
       const style = document.createElement('style');
       style.textContent = `
           .kylin-modals {
               position: fixed;
               top: 0;
               left: 0;
               right: 0;
               bottom: 0;
               z-index: 9999;
               pointer-events: none;
           }
           .kylin-modal-backdrop {
               position: absolute;
               top: 0;
               left: 0;
               right: 0;
               bottom: 0;
               background: rgba(0, 0, 0, 0.5);
               pointer-events: auto;
           }
           .kylin-modal-content {
               position: relative;
               pointer-events: auto;
               z-index: 1;
           }
       `;
       document.head.appendChild(style);
   }
   ```

在 attach() 方法中调用 initModals()。
  </action>
  <verify>grep -n "modalContainer\|modalState\|createModalContainer\|getModalConfig" src/router.ts</verify>
  <done>KylinRouter 实现模态容器管理，包括容器创建和事件监听</done>
</task>

<task type="auto">
  <name>Task 3: 实现 KylinRouter 的模态栈管理</name>
  <files>src/router.ts</files>
  <action>
在 KylinRouter 类中添加模态栈管理功能：

**核心方法：**

1. **打开模态（D-16）**
   ```typescript
   async openModal(options: ModalOptions): Promise<void> {
       // 解析路由配置
       const route = this.resolveModalRoute(options);
       if (!route) {
           throw new Error('Invalid modal route');
       }
       
       // 检查模态配置
       const modalConfig = this.getModalConfig(route);
       if (!modalConfig?.modal) {
           throw new Error('Route is not configured as modal');
       }
       
       // 检查模态栈限制
       if (this.modalState.stack.length >= this.maxModals) {
           console.warn('Maximum modal stack depth reached');
           return;
       }
       
       // 创建模态元素
       const modalElement = await this.createModalElement(route, modalConfig);
       
       // 创建背景遮罩（D-18）
       let backdropElement: HTMLElement | undefined;
       if (modalConfig.backdrop !== false) {
           backdropElement = this.createBackdrop(modalConfig);
       }
       
       // 添加到模态栈（D-19）
       const stackItem: ModalStackItem = {
           route,
           element: modalElement,
           backdrop: backdropElement,
           timestamp: Date.now()
       };
       this.modalState.stack.push(stackItem);
       this.modalState.current = stackItem;
       
       // 渲染模态内容
       await this.renderModal(stackItem);
       
       // 触发事件
       this.dispatchEvent(new CustomEvent('modal-open', {
           detail: { route, stackItem }
       }));
       
       // 更新 URL（不影响主页面的历史记录）
       this.updateModalURL(route);
   }
   ```

2. **关闭顶层模态（D-20）**
   ```typescript
   async closeTopModal(): Promise<void> {
       if (this.modalState.stack.length === 0) {
           return;
       }
       
       const stackItem = this.modalState.stack.pop();
       if (!stackItem) return;
       
       // 移除模态元素和遮罩
       stackItem.element.remove();
       stackItem.backdrop?.remove();
       
       // 更新当前模态
       this.modalState.current = this.modalState.stack[this.modalState.stack.length - 1] || null;
       
       // 触发事件
       this.dispatchEvent(new CustomEvent('modal-close', {
           detail: { route: stackItem.route, stackItem }
       }));
       
       // 恢复 URL
       this.restoreModalURL();
   }
   ```

3. **关闭所有模态**
   ```typescript
   async closeAllModals(): Promise<void> {
       while (this.modalState.stack.length > 0) {
           await this.closeTopModal();
       }
   }
   ```

4. **创建模态元素**
   ```typescript
   private async createModalElement(route: RouteItem, config: ModalConfig): Promise<HTMLElement> {
       const element = document.createElement('div');
       element.className = 'kylin-modal-content';
       
       // 加载组件
       const loadResult = await this.loader.loadComponent(route.component);
       if (loadResult.success) {
           // 渲染组件到模态元素
           await this.render.renderToOutlet(loadResult, element, {
               mode: 'replace'
           });
       }
       
       return element;
   }
   ```

5. **创建背景遮罩（D-18）**
   ```typescript
   private createBackdrop(config: ModalConfig): HTMLElement {
       const backdrop = document.createElement('div');
       backdrop.className = 'kylin-modal-backdrop';
       
       // 点击遮罩关闭模态
       if (config.closeOnBackdropClick !== false) {
           backdrop.addEventListener('click', () => {
               this.closeTopModal();
           });
       }
       
       return backdrop;
   }
   ```

6. **渲染模态**
   ```typescript
   private async renderModal(stackItem: ModalStackItem): Promise<void> {
       const container = this.modalContainer!;
       
       // 添加背景遮罩
       if (stackItem.backdrop) {
           container.appendChild(stackItem.backdrop);
       }
       
       // 添加模态内容
       container.appendChild(stackItem.element);
   }
   ```

**辅助方法：**
```typescript
private resolveModalRoute(options: ModalOptions): RouteItem | null
private updateModalURL(route: RouteItem): void
private restoreModalURL(): void
```

确保模态栈的正确管理和清理。
  </action>
  <verify>grep -n "openModal\|closeTopModal\|closeAllModals\|createModalElement\|createBackdrop" src/router.ts</verify>
  <done>KylinRouter 实现模态栈管理，支持打开、关闭和多层模态</done>
</task>

<task type="auto">
  <name>Task 4: 集成模态路由到导航流程</name>
  <files>src/router.ts</files>
  <action>
在 KylinRouter 类中集成模态路由到导航流程：

**修改点：**

1. **在 onRouteUpdate 方法中添加模态路由检测**
   ```typescript
   private async onRouteUpdate(location: Update) {
       // 路由匹配
       const matchedRoute = this.routes.match(location.pathname);
       if (!matchedRoute) return;
       
       // 检查是否为模态路由（D-16）
       const modalConfig = this.getModalConfig(matchedRoute);
       if (modalConfig?.modal) {
           // 模态路由：使用模态容器渲染
           await this.openModal({
               route: matchedRoute,
               backdrop: modalConfig.backdrop
           });
           return;
       }
       
       // 普通路由：正常渲染流程
       // ... 现有代码 ...
   }
   ```

2. **修改 back() 方法支持关闭模态（D-20）**
   ```typescript
   back(): void {
       // 如果有打开的模态，先关闭模态
       if (this.modalState.stack.length > 0) {
           this.closeTopModal();
           return;
       }
       
       // 没有模态，执行普通的后退导航
       this.history.back();
   }
   ```

3. **添加 closeModal 方法（D-20）**
   ```typescript
   async closeModal(route?: RouteItem | string): Promise<void> {
       if (route) {
           // 关闭指定模态
           const targetRoute = typeof route === 'string'
               ? this.routes.match(route)
               : route;
           const index = this.modalState.stack.findIndex(
               item => item.route === targetRoute
           );
           if (index !== -1) {
               // 关闭该模态及上面的所有模态
               while (this.modalState.stack.length > index) {
                   await this.closeTopModal();
               }
           }
       } else {
           // 关闭顶层模态
           await this.closeTopModal();
       }
   }
   ```

4. **添加模态状态查询方法**
   ```typescript
   get hasOpenModals(): boolean {
       return this.modalState.stack.length > 0;
   }
   
   get modalCount(): number {
       return this.modalState.stack.length;
   }
   
   get currentModal(): ModalStackItem | null {
       return this.modalState.current;
   }
   ```

5. **处理模态路由与普通路由的共存（D-16）**
   - 模态路由使用独立的容器，不影响普通路由的渲染
   - 模态路由更新 URL 时保持主路由的历史记录
   - 关闭模态时恢复主路由的 URL

**事件增强：**
- 'modal-open': 模态打开时触发
- 'modal-close': 模态关闭时触发
- 'modal-stack-change': 模态栈变化时触发

确保模态路由与普通路由的正确隔离和共存。
  </action>
  <verify>grep -n "onRouteUpdate\|back()\|closeModal\|hasOpenModals" src/router.ts</verify>
  <done>模态路由集成到导航流程，支持与普通路由共存</done>
</task>

<task type="auto">
  <name>Task 5: 增强 KylinOutletElement 支持模态路由</name>
  <files>src/components/outlet/index.ts</files>
  <action>
在 KylinOutletElement 中添加模态路由支持：

**新增属性：**
```typescript
@property({ type: Boolean, reflect: true })
isModal: boolean = false;  // 是否为模态 outlet
```

**实现要点：**

1. **模态 outlet 的特殊处理**
   ```typescript
   connectedCallback() {
       super.connectedCallback();
       if (this.isModal) {
           this.setupModalBehavior();
       }
   }
   
   private setupModalBehavior() {
       // 模态 outlet 的特殊行为
       this.style.position = 'relative';
       this.style.zIndex = '1';
   }
   ```

2. **模态路由变化监听**
   ```typescript
   private _handleRouteChange(event: CustomEvent) {
       const { route, router } = event.detail;
       
       // 检查是否为模态路由
       const isModalRoute = router.getModalConfig(route)?.modal;
       
       if (isModalRoute && !this.isModal) {
           // 模态路由但不是模态 outlet，不渲染
           return;
       }
       
       if (!isModalRoute && this.isModal) {
           // 普通路由但是模态 outlet，不渲染
           return;
       }
       
       // 渲染逻辑...
   }
   ```

3. **模态关闭监听**
   ```typescript
   private _setupModalCloseListener() {
       this.addEventListener('close-modal', () => {
           this.router.closeModal();
       });
   }
   ```

**使用示例：**
```html
<!-- 普通 outlet -->
<kylin-outlet path="/"></kylin-outlet>

<!-- 模态 outlet（在模态容器中） -->
<kylin-outlet isModal path="/modal"></kylin-outlet>
```

确保模态 outlet 和普通 outlet 的正确隔离。
  </action>
  <verify>grep -n "isModal\|setupModalBehavior\|_setupModalCloseListener" src/components/outlet/index.ts</verify>
  <done>KylinOutletElement 支持模态路由，正确处理模态和普通路由的隔离</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>完整的模态路由系统，包括：
1. 模态容器管理（.kylin-modals 容器）
2. 模态栈管理（支持多层模态）
3. 背景遮罩和关闭交互
4. 与普通路由的共存
5. 模态路由配置（modal 字段）
6. 打开/关闭模态的方法（openModal、closeModal、back）</what-built>
  <how-to-verify>
验证步骤：

1. **启动开发服务器**
   ```bash
   bun run dev
   ```

2. **访问模态路由示例页面**
   - 打开浏览器访问：http://localhost:5173/app/modal-demo.html
   - 如果文件不存在，创建基本的测试页面

3. **测试基本模态功能**
   - 点击"打开模态"按钮
   - 验证模态框在专用容器中显示（.kylin-modals）
   - 验证背景遮罩正确显示
   - 验证普通页面内容仍然可见

4. **测试关闭交互**
   - 点击背景遮罩，验证模态关闭
   - 按 ESC 键，验证模态关闭
   - 调用 router.back()，验证模态关闭
   - 调用 router.closeModal()，验证模态关闭

5. **测试多层模态（MODAL-03）**
   - 在第一个模态中打开第二个模态
   - 验证第二个模态覆盖在第一个模态上
   - 验证关闭第二个模态后，第一个模态仍然显示
   - 验证背景遮罩的层级关系

6. **测试与普通路由的共存（MODAL-01）**
   - 在普通路由页面打开模态
   - 验证普通路由的 URL 保持不变
   - 验证普通路由的内容仍然可见
   - 关闭模态后，验证普通路由正常工作

7. **测试模态路由配置**
   - 使用路由配置中的 modal: true 声明模态路由
   - 使用 backdrop: false 隐藏背景遮罩
   - 使用 closeOnBackdropClick: false 禁止点击关闭

8. **检查浏览器控制台**
   - 验证没有错误信息
   - 验证模态事件的触发（modal-open、modal-close）
   - 验证模态栈的正确管理

9. **验证样式和布局**
   - 检查模态框居中显示
   - 检查背景遮罩的半透明效果
   - 检查模态框的 z-index 层级

10. **测试边界情况**
    - 快速连续打开多个模态
    - 在模态打开时导航到普通路由
    - 在模态打开时刷新页面
  </how-to-verify>
  <resume-signal>模态路由功能验证通过，或描述发现的问题</resume-signal>
</task>

<task type="auto">
  <name>Task 7: 编写模态路由功能的单元测试</name>
  <files>src/__tests__/router.modal.test.ts</files>
  <action>
创建 `src/__tests__/router.modal.test.ts` 文件：

**测试覆盖：**

1. **模态容器管理测试**
   - 测试模态容器的创建
   - 测试模态容器的样式和结构
   - 测试模态容器的清理

2. **模态栈管理测试**
   - 测试打开模态并添加到栈
   - 测试关闭模态并从栈移除
   - 测试多层模态的正确堆叠
   - 测试模态栈的最大深度限制

3. **背景遮罩测试（MODAL-02）**
   - 测试背景遮罩的创建和显示
   - 测试点击遮罩关闭模态
   - 测试禁用点击关闭的行为
   - 测试 ESC 键关闭模态

4. **模态路由与普通路由共存测试（MODAL-01）**
   - 测试模态路由不影响普通路由渲染
   - 测试模态打开时普通路由 URL 保持不变
   - 测试关闭模态后普通路由正常工作

5. **多层模态测试（MODAL-03）**
   - 测试打开多个模态的正确堆叠
   - 测试关闭顶层模态后显示下层模态
   - 测试模态栈的正确管理

6. **关闭方法测试**
   - 测试 router.back() 关闭模态（D-20）
   - 测试 router.closeModal() 关闭模态
   - 测试 router.closeAllModals() 关闭所有模态

7. **集成测试**
   - 测试完整的模态路由流程
   - 测试模态路由的导航流程
   - 测试模态事件触发

使用 happy-dom 模拟浏览器环境，mock router 和 modal 行为。

每个测试用例包含：arrange、act、assert 三个部分。
  </action>
  <verify>bun test src/__tests__/router.modal.test.ts</verify>
  <done>所有模态路由测试通过，覆盖率达到 80% 以上</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modal config → router | 开发者配置的模态选项 |
| user interaction → modal close | 用户触发的关闭操作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Spoofing | openModal | accept | 模态路由由开发者配置，无欺骗风险 |
| T-03-20 | Tampering | modal stack | mitigate | 使用不可变的栈操作，验证模态配置的完整性 |
| T-03-21 | Repudiation | closeModal | accept | 模态关闭操作不涉及不可抵赖性需求 |
| T-03-22 | Information Disclosure | modal content | mitigate | 防止模态内容泄露敏感信息，确保模态在正确的安全上下文中打开 |
| T-03-23 | Denial of Service | modal stack | mitigate | 限制最大模态层数（默认 10），防止无限堆叠导致内存泄漏 |
| T-03-24 | Elevation of Privilege | openModal | accept | 模态在浏览器沙箱中执行，无权限提升风险 |

**Additional Security Considerations:**
- 模态栈有最大深度限制，防止资源耗尽
- 背景遮罩的点击事件有安全检查
- 模态容器的 z-index 使用固定值，避免被恶意覆盖
- 模态内容在独立的容器中渲染，隔离主页面内容
</threat_model>

<verification>
1. 模态容器管理验证：模态容器正确创建和管理
2. 模态栈验证：模态栈的正确管理，支持多层模态
3. 背景遮罩验证：背景遮罩正确显示和交互
4. 与普通路由共存验证：模态不影响普通路由
5. 关闭交互验证：各种关闭方式正常工作
6. 集成测试：完整模态路由流程正常工作
</verification>

<success_criteria>
1. 路由器支持模态路由渲染，在专用容器中打开模态框（MODAL-01）
2. 模态路由支持背景遮罩和关闭交互（MODAL-02）
3. 模态路由可以与普通路由共存（MODAL-01）
4. 支持多层模态栈，新模态覆盖旧模态（MODAL-03）
5. 用户可以通过 router.back() 或 router.closeModal() 关闭模态（D-20）
6. 单元测试覆盖率达到 80% 以上
7. 模态路由与 Loader、Render、DataLoader 系统正确集成
</success_criteria>

<output>
After completion, create `.planning/phases/03-loader-render-data/03-04-SUMMARY.md`
</output>
