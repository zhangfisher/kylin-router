---
phase: refactor-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/router.ts]
autonomous: true
requirements: [REFACTOR-01]
user_setup: []

must_haves:
  truths:
    - "router.ts 中所有路由管理逻辑移除，通过 Routes mixin 提供"
    - "重复代码删除，仅保留 KylinRouter 特有的逻辑"
    - "所有功能保持完全一致"
    - "类型定义保持兼容"
    - "测试全部通过"
  artifacts:
    - path: "src/router.ts"
      provides: "重构后的主路由器类"
      min_lines: 400
    - path: "src/features/routes.ts"
      provides: "路由管理功能 mixin"
      unchanged: true
  key_links:
    - from: "src/router.ts"
      to: "src/features/routes.ts"
      via: "Mixin 继承"
      pattern: "extends Mixin.*Routes"
---

<objective>
重构 router.ts，移除与 Routes mixin 重复的代码，确保路由管理功能完全通过 Routes mixin 提供。

目的：消除代码重复，提高可维护性，确保所有路由管理功能统一在 Routes mixin 中实现。
输出：清理后的 router.ts，仅保留 KylinRouter 特有的核心逻辑
</objective>

<execution_context>
@E:/Work/Code/kylin-router/.claude/get-shit-done/workflows/execute-plan.md
@E:/Work/Code/kylin-router/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/router.ts
@src/features/routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 router.ts 中的重复属性定义</name>
  <files>src/router.ts</files>
  <action>
删除 router.ts 中以下重复属性（第 41-63 行），这些属性已在 Routes mixin 中定义：
- routes: RouteItem[] （第 42 行）
- notFound?: RouteItem （第 44 行）  
- defaultRoute?: string （第 47 行）
- currentRoute: MatchedRoute | null （第 51 行）
- params: Record<string, string> （第 53 行）
- query: Record<string, string> （第 56 行）
- _redirectCount: number （第 63 行）
- MAX_REDIRECTS 常量（第 66 行）

保留 KylinRouter 特有的属性：
- _cleanups （第 35 行）
- host （第 36 行）
- history （第 37 行）
- outlets （第 39 行）
- isNavigating （第 59 行）
- _pendingNavigationType （第 285 行）

注意：Routes mixin 已经定义了所有路由相关的属性，通过 Mixin 继承机制这些属性会自动合并到 KylinRouter 类中。
  </action>
  <verify>
    grep -n "routes:\|notFound:\|defaultRoute:\|currentRoute:\|params:\|query:\|_redirectCount:\|MAX_REDIRECTS" src/router.ts
    # 应该没有结果，所有这些属性都已被删除
  </verify>
  <done>
    重复属性已全部删除，路由相关属性通过 Routes mixin 提供
  </done>
</task>

<task type="auto">
  <name>Task 2: 删除重复的辅助函数</name>
  <files>src/router.ts</files>
  <action>
删除 router.ts 中的重复辅助函数（第 454-484 行）：
- normalizeRoutes 函数（第 458-464 行）- 已在 routes.ts 中定义
- removeRouteByName 函数（第 471-484 行）- 已在 routes.ts 中定义

这两个辅助函数仅在 Routes mixin 内部使用，不应该在 router.ts 中重复定义。
  </action>
  <verify>
    grep -n "normalizeRoutes\|removeRouteByName" src/router.ts
    # 应该没有结果，这些函数已从 router.ts 中删除
  </verify>
  <done>
    重复的辅助函数已删除，仅保留在 Routes mixin 中的定义
  </done>
</task>

<task type="auto">
  <name>Task 3: 删除重复的方法实现</name>
  <files>src/router.ts</files>
  <action>
删除 router.ts 中与 Routes mixin 重复的方法：
1. addRoute 方法（第 370-378 行）- 已在 Routes mixin 中实现
2. removeRoute 方法（第 386-393 行）- 已在 Routes mixin 中实现
3. loadRemoteRoutes 方法（第 400-420 行）- 已在 Routes mixin 中实现
4. _checkDefaultRedirect 方法（第 214-243 行）- 已在 Routes mixin 中实现
5. _redirectToDefaultOrNotFound 方法（第 426-437 行）- 已在 Routes mixin 中实现
6. _matchCurrentLocation 方法（第 249-282 行）- 已在 Routes mixin 中实现

这些方法通过 Mixin 继承会自动合并到 KylinRouter 类中，不需要在 router.ts 中重复定义。
  </action>
  <verify>
    grep -n "addRoute\|removeRoute\|loadRemoteRoutes\|_checkDefaultRedirect\|_redirectToDefaultOrNotFound\|_matchCurrentLocation" src/router.ts
    # 应该没有结果，这些方法已从 router.ts 中删除
  </verify>
  <done>
    重复方法已全部删除，路由管理方法通过 Routes mixin 提供
  </done>
</task>

<task type="auto">
  <name>Task 4: 简化构造函数中的路由初始化逻辑</name>
  <files>src/router.ts</files>
  <action>
修改构造函数（第 73-139 行），简化路由初始化逻辑：
1. 调用 Routes mixin 的 initRoutes 方法而不是直接在构造函数中处理
2. 删除构造函数中的路由初始化代码（第 89-125 行）
3. 在构造函数最后调用 this.initRoutes() 方法

保留构造函数中的特有逻辑：
- host 元素的获取和验证
- history 实例的创建
- context provider 的设置

修改后的构造函数应该：
1. 接收参数并规范化
2. 创建 history 实例
3. 设置 host 元素
4. 调用 this.initRoutes() 初始化路由表
5. 执行 this._matchCurrentLocation() 进行初始匹配
  </action>
  <verify>
    grep -A5 "initRoutes" src/router.ts | head -20
    # 应该能看到 this.initRoutes() 的调用
  </verify>
  <done>
    构造函数简化，路由初始化委托给 Routes mixin 的 initRoutes 方法
  </done>
</task>

<task type="auto">
  <name>Task 5: 验证重构后的功能完整性</name>
  <files>src/router.ts, src/__tests__/*.test.ts</files>
  <action>
运行所有测试以验证重构后的功能完整性：
1. 运行测试套件：bun test
2. 确保所有测试通过
3. 特别关注以下测试：
   - router.dynamic.test.ts (addRoute/removeRoute)
   - router.remote.test.ts (loadRemoteRoutes)
   - router.redirect.test.ts (_checkDefaultRedirect)
   - router.navigation.test.ts (路由匹配)

如果有测试失败，分析失败原因并修复：
- 检查是否遗漏了必要的代码
- 确认 Mixin 继承是否正确合并了方法
- 验证属性访问是否正确（通过 this 访问 mixin 提供的属性）
  </action>
  <verify>
    bun test
    # 所有测试应该通过
  </verify>
  <done>
    所有测试通过，重构后的功能与重构前完全一致
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 代码重构 | 重构不应改变任何外部行为，仅改变内部实现 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-refactor-01 | Tampering | router.ts | mitigate | 确保所有删除的代码都能通过 mixin 正确访问，使用测试验证功能不变 |
| T-refactor-02 | Information Disclosure | router.ts | accept | 删除重复代码不会暴露任何敏感信息，只是清理冗余实现 |
| T-refactor-03 | Spoofing | router.ts | mitigate | 确保 mixin 继承正确，不会出现方法被错误覆盖的情况，通过测试验证 |
</threat_model>

<verification>
## 重构验证清单

- [ ] 所有重复属性已从 router.ts 删除
- [ ] 所有重复方法已从 router.ts 删除
- [ ] 所有重复辅助函数已从 router.ts 删除
- [ ] 构造函数已简化，路由初始化委托给 Routes mixin
- [ ] Mixin 继承正确，所有路由管理功能可正常访问
- [ ] 所有测试通过（bun test）
- [ ] 类型检查通过（bun run build 或 tsc --noEmit）
- [ ] 代码编译成功（bun run build）
</verification>

<success_criteria>
## 成功标准

1. **代码重复消除**: router.ts 中不再有与 Routes mixin 重复的代码
2. **功能完全一致**: 所有测试通过，功能行为与重构前完全相同
3. **类型安全**: TypeScript 类型检查通过，无类型错误
4. **编译成功**: 代码可以正常编译和构建
5. **代码简化**: router.ts 行数减少约 30%，代码更清晰易维护

## 预期结果

- router.ts 从 485 行减少到约 350 行
- 所有路由管理功能统一通过 Routes mixin 提供
- 代码结构更清晰，职责更明确
- 维护性提升，路由相关修改只需要在 Routes mixin 中进行
</success_criteria>

<output>
重构完成后，创建 `.planning/phases/refactor-routes/refactor-routes-01-SUMMARY.md` 包含：
- 删除的代码行数和位置
- 测试结果
- 遇到的问题和解决方案
- 重构前后的对比
</output>
