---
phase: quick
plan: 260408-te8
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/triggerEvent.ts
  - src/router.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "所有事件派发统一使用 triggerEvent 工具函数"
    - "工具函数名拼写错误已修正"
    - "代码一致性提升，便于后续维护"
  artifacts:
    - path: "src/utils/triggerEvent.ts"
      provides: "统一的事件派发工具函数"
      min_lines: 10
    - path: "src/router.ts"
      provides: "使用统一工具函数的路由器"
      contains: "triggerEvent"
  key_links:
    - from: "src/router.ts"
      to: "src/utils/triggerEvent.ts"
      via: "import statement"
      pattern: "import.*triggerEvent.*from.*triggerEvent"
---

<objective>
统一使用 triggerEvent 工具函数代替直接调用 host.dispatchEvent，并修正函数名拼写错误

Purpose: 提高代码一致性，减少重复代码，便于后续维护和修改事件派发逻辑
Output: 修正后的工具函数和更新后的路由器文件
</objective>

<execution_context>
@E:\Work\Code\kylin-router\.claude\get-shit-done\workflows\execute-plan.md
@E:\Work\Code\kylin-router\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\Work\Code\kylin-router\.planning\STATE.md
@E:\Work\Code\kylin-router\CLAUDE.md
@E:\Work\Code\kylin-router\src\utils\triggerEvent.ts
@E:\Work\Code\kylin-router\src\router.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修正 triggerEvent 函数名拼写错误</name>
  <files>src/utils/triggerEvent.ts</files>
  <action>
    1. 将函数名从 `trigggerEvent` 修正为 `triggerEvent`（移除多余的 'g'）
    2. 确保函数签名和实现保持不变
    3. 导出语句使用正确的函数名
  </action>
  <verify>
    <automated>grep -n "export function triggerEvent" src/utils/triggerEvent.ts</automated>
  </verify>
  <done>函数名拼写错误已修正，导出语句正确</done>
</task>

<task type="auto">
  <name>Task 2: 更新 router.ts 中的 import 语句</name>
  <files>src/router.ts</files>
  <action>
    1. 修改第 22 行的 import 语句：从 `trigggerEvent` 改为 `triggerEvent`
    2. 确保 import 语句指向正确的路径和函数名
  </action>
  <verify>
    <automated>grep -n "import.*triggerEvent.*from" src/router.ts</automated>
  </verify>
  <done>import 语句已更新为正确的函数名</done>
</task>

<task type="auto">
  <name>Task 3: 统一 router.ts 中所有事件派发调用</name>
  <files>src/router.ts</files>
  <action>
    找出所有使用 `this.host.dispatchEvent(new CustomEvent(...))` 的地方，替换为 `triggerEvent(this.host, eventName, detail)`：
    
    1. 第 331-341 行：route-change 事件
    2. 第 373-381 行：navigation-end 事件  
    3. 第 433-441 行：navigation-start 事件（push 方法）
    4. 第 454-462 行：navigation-start 事件（replace 方法）
    5. 第 475-484 行：navigation-start 事件（back 方法）
    6. 第 493-500 行：navigation-start 事件（forward 方法）
    7. 第 509-517 行：navigation-start 事件（go 方法）
    
    注意：第 485 行已经使用了 trigggerEvent，需要改为 triggerEvent
  </action>
  <verify>
    <automated>grep -n "this\.host\.dispatchEvent" src/router.ts | wc -l</automated>
  </verify>
  <done>所有 host.dispatchEvent 调用已替换为 triggerEvent 工具函数</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router → host | 路由器向宿主元素派发事件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | T | triggerEvent function | accept | 工具函数，无安全风险，事件数据由调用方控制 |
| T-quick-02 | I | event detail data | accept | 事件数据不包含敏感信息，仅为路由元数据 |
</threat_model>

<verification>
1. 所有 import 语句使用正确的函数名
2. 不再存在直接调用 host.dispatchEvent 的代码
3. TypeScript 编译通过
4. 事件派发功能保持不变
</verification>

<success_criteria>
- triggerEvent 函数名拼写正确
- router.ts 中所有 7 处事件派发统一使用 triggerEvent
- 代码能够正常编译运行
- 事件派发行为与重构前完全一致
</success_criteria>

<output>
After completion, create `.planning/quick/260408-te8-triggerevent-host-dispatchevent/260408-te8-SUMMARY.md`
</output>
