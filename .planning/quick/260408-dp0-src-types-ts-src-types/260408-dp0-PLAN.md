---
phase: quick-task
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types.ts, src/types/index.ts, src/types/route.ts, src/types/hooks.ts, src/types/config.ts]
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "所有现有类型定义保持完全兼容"
    - "保留现有 src/types/hooks.ts 的结构（BeforeEachHook 等）"
    - "整合 types.ts 中的 HookType 和现有 KylinRouterHookType"
    - "导入路径从 'src/types.ts' 更新为 'src/types'"
    - "类型按功能分类到独立文件中"
    - "构建和类型检查无错误"
  artifacts:
    - path: "src/types/index.ts"
      provides: "统一导出所有类型，保持向后兼容"
      exports: ["RouteItem", "KylinRoutes", "MatchedRoute", "HookFunction", "HookType", "RouteData", "RenderEachHook", "BeforeEachHook", "AfterEachHook", "KylinRouterHooks", "KylinRouterHookType", "KylinRouterOptiopns"]
    - path: "src/types/route.ts"
      provides: "路由相关的类型定义"
      contains: ["RouteItem", "KylinRoutes", "MatchedRoute"]
    - path: "src/types/hooks.ts"
      provides: "钩子相关的类型定义（扩展版）"
      contains: ["BeforeEachHook", "RenderEachHook", "AfterEachHook", "KylinRouterHooks", "KylinRouterHookType", "HookFunction", "HookType", "RouteData"]
    - path: "src/types/config.ts"
      provides: "配置相关的类型定义"
      contains: ["KylinRouterOptiopns"]
  key_links:
    - from: "src/types/index.ts"
      to: "src/types/route.ts"
      via: "re-export"
      pattern: "export.*from.*route"
    - from: "src/types/index.ts"
      to: "src/types/hooks.ts"
      via: "re-export"
      pattern: "export.*from.*hooks"
    - from: "src/types/index.ts"
      to: "src/types/config.ts"
      via: "re-export"
      pattern: "export.*from.*config"
---

<objective>
将 `src/types.ts` 重构为 `src/types/` 文件夹，按功能分类保存类型定义

**目的：** 提高代码可维护性，将大型类型文件拆分为按功能组织的模块结构

**特别要求：** 保留现有的 `src/types/hooks.ts` 文件结构，用 `src/types.ts` 中的类型扩展它

**输出：**
- `src/types/index.ts` - 统一导出所有类型（保持向后兼容）
- `src/types/route.ts` - 路由相关类型
- `src/types/hooks.ts` - 钩子相关类型（扩展现有文件）
- `src/types/config.ts` - 配置相关类型
- 删除原 `src/types.ts` 文件
</objective>

<execution_context>
@C:/Work/Code/kylin-router/.claude/get-shit-done/workflows/execute-plan.md
@C:/Work/Code/kylin-router/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/types.ts
@src/types/hooks.ts (现有文件，需保留并扩展)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建和扩展类型文件</name>
  <files>src/types/route.ts, src/types/hooks.ts, src/types/config.ts, src/types/index.ts</files>
  <action>
根据 `src/types.ts` 的内容，将类型按功能分类创建到独立文件中：

1. **src/types/route.ts** - 路由相关类型：
   - `RouteItem` 接口（路由配置项）
   - `KylinRoutes` 类型（路由配置集合）
   - `MatchedRoute` 接口（路由匹配结果）

2. **src/types/hooks.ts** - 钩子相关类型（保留并扩展现有文件）：
   - **保留现有类型：**
     - `BeforeEachHook` - beforeEach 钩子函数类型
     - `RenderEachHook` - renderEach 钩子函数类型
     - `AfterEachHook` - afterEach 钩子函数类型
     - `KylinRouterHooks` - 钩子集合对象类型
     - `KylinRouterHookType` - 钩子类型的键

   - **从 types.ts 添加类型：**
     - `HookFunction` - 通用钩子函数类型（与 BeforeEachHook 类似但更通用）
     - `HookType` - 钩子类型常量对象（包含 BEFORE_EACH, RENDER_EACH, AFTER_EACH）
     - `RouteData` - 预加载数据类型（Record<string, any>）

   - **整合说明：**
     - HookType 常量对象与现有的 KylinRouterHookType 互补
     - 保留两者的导出，通过 JSDoc 说明使用场景
     - HookFunction 作为 BeforeEachHook 的更通用版本

3. **src/types/config.ts** - 配置相关类型：
   - `KylinRouterOptiopns` 接口（路由器配置选项）

4. **src/types/index.ts** - 统一导出：
   - 重新导出所有类型，保持向后兼容
   - 确保所有导出的类型名称与原 `src/types.ts` 完全一致

**重要：**
- 保留现有 hooks.ts 的完整结构和所有现有类型
- 保持所有 JSDoc 注释完整
- 整合时添加注释说明类型之间的关系
- 遵循项目命名约定：PascalCase 用于接口和类型
  </action>
  <verify>
    <automated>bun run build && bun test --type-check 2>&1 | head -50</automated>
  </verify>
  <done>四个类型文件创建完成，hooks.ts 已扩展，所有类型正确导出</done>
</task>

<task type="auto">
  <name>Task 2: 更新所有导入路径</name>
  <files>src/router.ts, src/components/base/index.ts, src/features/context.ts, src/features/hooks.ts</files>
  <action>
更新所有引用 `src/types.ts` 的文件，将导入路径从 `./types` 更新为 `./types/index` 或 `./types`：

1. 查找所有导入语句：
   ```bash
   grep -r "from.*['\"]./types['\"]" src/ --include="*.ts"
   ```

2. 对每个文件，更新导入路径：
   - 将 `from './types'` 更新为 `from './types'`（保持不变，但指向文件夹）
   - 或者显式使用 `from './types/index'`

3. 验证更新后的导入路径正确解析

**预期修改的文件：**
- `src/router.ts` - 导入 KylinRouterOptiopns, RouteItem 等
- `src/features/hooks.ts` - 导入钩子相关类型
- `src/components/base/index.ts` - 如果有类型导入
- 其他使用类型的文件

**注意：**
- 使用相对路径导入（保持 `./types` 格式）
- 不使用路径别名（尽管 tsconfig 配置了 `@/*`）
  </action>
  <verify>
    <automated>grep -r "from.*['\"]./types['\"]" src/ --include="*.ts" | wc -l</automated>
  </verify>
  <done>所有导入路径已更新，TypeScript 编译无错误</done>
</task>

<task type="auto">
  <name>Task 3: 删除原 types.ts 文件并验证</name>
  <files>src/types.ts</files>
  <action>
1. 删除原始的 `src/types.ts` 文件
2. 运行完整的类型检查和构建验证：
   ```bash
   bun run build
   ```
3. 确保所有类型定义仍然可访问且无错误
4. 运行测试（如果有）：
   ```bash
   bun test
   ```

**验证要点：**
- TypeScript 编译无类型错误
- 构建成功生成 dist 文件
- 所有类型导出正常工作
- 无运行时错误
  </action>
  <verify>
    <automated>bun run build 2>&1 && ls -la dist/ 2>&1 | head -20</automated>
  </verify>
  <done>原 types.ts 文件已删除，项目构建和类型检查均成功</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 构建系统 → 类型定义 | TypeScript 编译器读取类型文件进行类型检查 |
| 现有 hooks.ts → 新类型 | 保留现有结构，添加新类型而不破坏兼容性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QT-05-01 | Spoofing | 类型导入路径 | accept | 类型检查会在编译时捕获导入错误，无安全风险 |
| T-QT-05-02 | Tampering | 类型定义完整性 | mitigate | TypeScript 编译器验证类型一致性，重构后必须通过完整类型检查 |
| T-QT-05-03 | Information Disclosure | - | - | 无敏感信息，类型定义公开透明 |
| T-QT-05-04 | Tampering | hooks.ts 扩展兼容性 | mitigate | 保留所有现有类型和导出，仅添加新类型，通过完整测试验证 |

**安全策略：** 这是一个纯代码重构任务，主要风险是破坏类型系统的完整性或现有 hooks.ts 的兼容性。通过完整的 TypeScript 编译和构建验证来缓解。
</threat_model>

<verification>
## 整体验证

1. **类型完整性验证**
   - 运行 `bun run build` 确保无类型错误
   - 检查所有类型导出正确
   - 验证类型定义在 IDE 中正确显示

2. **导入路径验证**
   - 确认所有文件的导入路径正确
   - 验证无循环依赖
   - 检查构建输出正常

3. **向后兼容性验证**
   - 确认 `from './types'` 导入仍然工作
   - 验证所有公共 API 类型保持不变
   - 检查类型推导和智能提示正常

4. **hooks.ts 扩展验证**
   - 确认所有现有类型（BeforeEachHook 等）仍然可用
   - 验证新增类型（HookFunction, HookType, RouteData）正确导出
   - 检查类型关系和兼容性

## 自动化验证命令

```bash
# 类型检查
bun run build

# 搜索类型导入
grep -r "from.*types" src/ --include="*.ts"

# 验证文件结构
ls -la src/types/

# 验证 hooks.ts 导出
grep "export" src/types/hooks.ts
```
</verification>

<success_criteria>
## 完成标准

- [ ] `src/types/` 文件夹已创建，包含 4 个类型文件
- [ ] 所有类型按功能正确分类（route, hooks, config）
- [ ] `src/types/hooks.ts` 保留了所有现有类型并添加了新类型
- [ ] `src/types/index.ts` 统一导出所有类型
- [ ] 原始 `src/types.ts` 文件已删除
- [ ] 所有导入路径已更新并验证
- [ ] TypeScript 编译无错误（`bun run build` 成功）
- [ ] 类型定义在 IDE 中正确显示和提示
- [ ] 构建产物正常生成
- [ ] 向后兼容性保持（`from './types'` 仍然工作）
- [ ] hooks.ts 现有类型全部可用，新类型正确整合

## 质量标准

- 代码符合项目命名约定（PascalCase 接口和类型）
- 所有 JSDoc 注释保持完整
- 无类型重复或冲突
- 文件结构清晰，易于维护
- hooks.ts 扩展后保持类型一致性
</success_criteria>

<output>
完成后的总结文档：`.planning/quick/260408-dp0-src-types-ts-src-types/260408-dp0-SUMMARY.md`
</output>
