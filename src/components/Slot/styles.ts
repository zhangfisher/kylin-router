/**
 * KylinSlot 组件样式
 *
 * @module components/Slot/styles
 */
export const styles = new CSSStyleSheet();
styles.replaceSync(`
    kylin-slot {
        display: block;
        position: relative;
        box-sizing: border-box;
        padding: 0 !important;
        margin: 0 !important;
    }
`);
